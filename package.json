{
  "name": "lacasastudio",
  "version": "2.2.0",
  "private": true,
  "description": "LaCasaStudio V2.2 — YouTube Production OS (TypeScript)",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "build:server": "cd server && npm run build",
    "start": "cd server && npm run start",
    "db:push": "cd server && npx prisma db push",
    "db:seed": "cd server && npm run seed",
    "setup": "npm install && cd server && npm install && npx prisma generate && npx prisma db push && npm run seed && cd ../client && npm install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
